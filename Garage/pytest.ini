[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
markers =
    unit: pure unit tests (no I/O)
    integration: tests that use the FastAPI TestClient
    slow: tests that hit the real database (skip in CI without DB)
